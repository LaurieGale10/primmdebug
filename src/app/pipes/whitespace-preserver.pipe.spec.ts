import { WhitespacePreserverPipe } from './whitespace-preserver.pipe';

describe('WhitespacePreserverPipe', () => {
  it('create an instance', () => {
    const pipe = new WhitespacePreserverPipe();
    expect(pipe).toBeTruthy();
  });
});
